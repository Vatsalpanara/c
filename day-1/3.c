#include <stdio.h>

int main() {
    int num = 10; // Initial value of the variable

    // Increment by 5 using the += operator
    num += 5;

    // Decrement by 2 using the -= operator
    num -= 2;

    // Print the final value of the variable
    printf("The final value of the variable is: %d\n", num);
}